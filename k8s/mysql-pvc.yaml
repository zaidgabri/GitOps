apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
spec:
  resources:
    requests:
      storage: 256Mi
  accessModes:
    - ReadWriteOnce
  StorageClassName: nil  # Change from &"" to nil
