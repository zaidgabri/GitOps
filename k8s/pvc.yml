apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
spec:
  resources:
    requests:
      storage: 256Mi
  storageClassName: disk.csi.azure.com  # Use an appropriate storage class
  accessModes:
    - ReadWriteOnce
